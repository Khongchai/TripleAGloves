try to get one component working with gatsby background image. Refactor it into a new file, too.

change the anchor tag to a normal div and use the id provided to take user to different sections + offset the fixed 
navbar instead.